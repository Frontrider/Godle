import io.github.frontrider.godle.dsl.versioning.VersionsKt
//We check that the plugin does not crash without setting anything.
plugins {
    id 'io.github.frontrider.godle'
}

godle{
    version = VersionsKt.godot(godle,godot_version)
}